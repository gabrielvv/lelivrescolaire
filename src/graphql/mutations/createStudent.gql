mutation CreateStudent($input: CreateStudentInput) {
    createStudent(input: $input) @client {
        lastname
        firstname
        email
        classId
        lessons {
            completion
            successRate
        }
    }
}